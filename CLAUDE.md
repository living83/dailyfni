# 프로젝트 메모

## 실행 방법
- Windows 환경에서 `run.bat`으로 실행 (naver-cafe-macro/run.bat)
- `python backend/main.py` 직접 실행 X → 반드시 `run.bat` 사용
- `run.bat`이 venv 생성, 패키지 설치, 서버 실행을 자동 처리

## 안내 시 주의
- 사용자에게 실행 안내할 때: `git pull` 후 `run.bat` 실행
